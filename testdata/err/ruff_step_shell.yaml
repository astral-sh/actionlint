on: push
jobs:
  step-level:
    runs-on: ubuntu-latest
    steps:
      # Lint check failed.
      - run: print(unknown_var)
        shell: python
      # Syntax check failed.
      - run: print(
        shell: python
      # Check custom shell
      - run: print(unknown_var)
        shell: 'python {0}'
